public class Main {
    public static void main(String[] args) {

        //Arrays are contiguous groupings of elements. These elements can be primitives or references.
        Object o = new Object();

        //dot (.) is the "de-reference" operator, and is used to "navigate" to the location on the heap
        //where the object that the reference points to can be found.
        o.toString();

    }
}
